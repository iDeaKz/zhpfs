{
  "name": "zhpfs-frontend",
  "version": "1.0.0",
  "dependencies": {
    "@babel/cli": "^7.15.7",
    "@babel/core": "^7.15.8",
    "@babel/preset-react": "^7.14.5",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "^5.0.1",
    "serve": "^14.0.1"
  },
  "scripts": {
    "audit": "npm audit fix",
    "build": "react-scripts build",
    "start": "npm run audit && npm run build && serve -s build -l 3000"
  }
}
